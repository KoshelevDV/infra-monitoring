---
galaxy_info:
  author: infra-team
  license: MIT
  role_name: ufw
  description: Configure UFW firewall
  min_ansible_version: "2.9"

# ufw must run after ssh_hardening so it uses the correct ssh_port
dependencies:
  - role: ssh_hardening
