---
galaxy_info:
  author: infra-team
  role_name: fail2ban
  description: Install and configure fail2ban for SSH protection
  min_ansible_version: "2.9"

# fail2ban must run after ufw (firewall must be up first)
dependencies:
  - role: ufw
